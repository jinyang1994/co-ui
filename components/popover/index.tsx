import Popover, { Props as PopoverProps } from './Popover';
import { Placement } from './Popper';

export { TOP, BOTTOM, LEFT, RIGHT, CLICK, FOCUS, HOVER, START, END } from './constants';
export { PopoverProps, Placement };
export default Popover;
